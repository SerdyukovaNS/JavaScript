<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<!--
    Не выполняя кода, ответить, что выведет браузер и почему:
-->
    <script>

        /* 
        if (!("a" in window)) {
            var a = 1;
        }
        alert(a);

        В данном примере браузер выведет undefined потому что по условию if строка должна быть неистинной, т.е. без значения, а она имеет числовое значение 1, значит условие не соблюдается, а на этот случай условия вывода нет (undefined), что и выводится в alert.*/

        /* 
        var b = function a(x) {
        x && a(--x);
        };
        alert(a);

        В данном примере браузер не выведет ничего, т.к. "а" - это имя функции и в теле функции не определено какое-либо действие. */

        /*
        function a(x) {
            return x * 2;
        }
        var a;
        alert(a);

        В данном примере браузер не выведет ничего, т.к. переменная "a" объявлена без указания значения, а значит в "alert" выводится функция "а", для которой значение переменной "x", указанной в функции, отсутствует.*/

        /*
        function b(x, y, a) {
            arguments[2] = 10;
            alert(a);
        }
        b(1, 2, 3);

        Браузер выведет в "alert" значение третьего аргумента, которому присвоено значение 10 в теле функции.*/

        /*
        function a() {
            alert(this);
        }
        a.call(null);

        "this" обозначает объект, который запускает данную функцию. Здесь объект отсутствует, значит "this" ничего не обозначает. Браузер понимает, что должен быть объект, но не видит какой. В "alert" будет указано, что это объект Window.*/


    </script>
    
</body>
</html>
